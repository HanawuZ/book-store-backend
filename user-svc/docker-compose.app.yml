version: '3.9'

services:
 user_svc:
  container_name: user_svc
  build:
    context: .
    dockerfile: Dockerfile
  networks:
    - bookshop-network


  # Map local machine port to Container port
  ports:
    - "5045:5045"

networks:
  bookshop-network:
    external: true